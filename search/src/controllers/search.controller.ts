import { NextFunction, Request, Response } from "express";
export const getNearby = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  try {
  } catch (err) {
    next(err);
  }
};
